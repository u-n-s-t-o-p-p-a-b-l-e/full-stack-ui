Simple rust framework using Rocket and Tera templates.

Make `templates` folder in the root directory:

```
mkdir templates
```
After updating Cargo.toml, run it with:

```
cargo run
```

For easy hot reloading:

```
cargo watch -x run 
```
